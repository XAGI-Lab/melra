{
  "name": "@melra/receipt-schema",
  "version": "0.3.0-alpha.9",
  "description": "Canonical receipts, redaction, and execution certificates for MELRA.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/XAGI-Lab/melra.git",
    "directory": "packages/receipt-schema"
  },
  "homepage": "https://github.com/XAGI-Lab/melra#readme",
  "bugs": "https://github.com/XAGI-Lab/melra/issues",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
